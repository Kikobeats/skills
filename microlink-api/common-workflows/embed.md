# Direct embed output

```html
<img src="https://api.microlink.io/?url=https://example.com&screenshot=true&meta=false&embed=screenshot.url">
```

Useful embed paths: `screenshot.url`, `pdf.url`, `image.url`, `logo.url`, `video.url`.
